<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.0/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         stopOnFailure="false"
         requireCoverageMetadata="false"
         beStrictAboutCoverageMetadata="false">
    <testsuites>
        <testsuite name="unit">
            <directory>tests/unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/integration</directory>
        </testsuite>
    </testsuites>
    <php>
        <!-- Integration tests drive Application::run() in-process (no subprocess boundary to
             hide a real terminal behind), so the progress bar's own TTY auto-detection would
             otherwise make captured output depend on whether the suite happens to be run from an
             interactive terminal or a piped/CI context. See Application::parseArgs(). -->
        <env name="WP_SPECTER_NO_PROGRESS" value="1"/>
    </php>
</phpunit>
